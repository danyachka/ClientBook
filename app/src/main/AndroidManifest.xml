<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ClientBook"
        tools:targetApi="31">
        <activity
            android:name=".ui.activities.ClientActivity"
            android:exported="false"
            android:theme="@style/Theme.ClientBook.NoActionBar" />
        <activity
            android:name=".ui.activities.appointment_creation.AppointmentCreationActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.ClientListActivity"
            android:exported="false"
            android:theme="@style/AppSlideTheme" />
        <activity
            android:name=".ui.activities.ClientCreationActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>